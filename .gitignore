# Hermes runtime zone — keys, rosters, and anything pasted or dropped stay
# on your machine. Never commit this directory.
hermes/app/runtime/
hermes/app/.env
.env
hermes/app/roster.csv

# Python
__pycache__/
*.pyc

# Prolog compiled state
*.qlf

# macOS
.DS_Store

# Bundle build output
#build/

# LaTeX build byproducts (the appendix source ships; its build residue does not)
*.aux
*.bbl
*.bcf
*.blg
*.run.xml
*.synctex.gz
.pytest_cache/
.superpowers/
scripts/research/talkmoves_rerun_out/
CLAUDE.md
.bigred-output/
.bigred-collected/
scripts/research/articulation_out/
scripts/research/quantity_binding_out/
scripts/research/negative_receipts_out/
scripts/research/vision_excerpt_verification_out/
scripts/research/incompatibility_triples_out/
.claude/worktrees/
.claude/

# SQLite write-ahead log and shared-memory files: process-local, never shipped
data/research/*.db-wal
data/research/*.db-shm

# Atlas sweep intermediates. The landscape, the fact store, and the summary are
# the durable outputs; the per-lesson shards and the cell list are rebuilt by
# every run and carry nothing the merged landscape does not.
scripts/bigred/iteration15/work/atlas/shards/
scripts/bigred/iteration15/work/atlas/cells.json

# Dump of all automata (gitignored)
automata.pl

# Held for review before publication
docs/research/2026-08-05-hyperedges-and-the-calculus-rung.html

# Local research log: per-task engineering reports, pass reviews, plans,
# and status snapshots. Findings stay tracked in docs/research/ above;
# git history keeps the record of everything that moved here.
docs/research/internal/
