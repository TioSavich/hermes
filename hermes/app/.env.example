# Copy this file to runtime/.env, or use the app's "Set key" field.
# Your REALLMS key is read from here; runtime/.env is gitignored.
REALLMS_API_KEY=sk-PASTE-YOUR-KEY-HERE

# Optional overrides:
# REALLMS_BASE_URL=https://reallms.rescloud.iu.edu/direct/v1
# REALLMS_MODEL=gemma-4-31B-it

# REALLMS_INSECURE=1 disables TLS verification — temporary debugging only.
# The app never sets it for you, and campus mode ignores it.
