command: swipl -q --on-warning=status --on-error=status -s <scratch>/candidate.pl -g <gate> -t halt
exit: 0
stdout:
GOT=frac(2,6)
EXPECTED=frac(1,3)
CLASS=incorrect
stderr:

description-match: matched error-action tokens: arbitrary, denominators, multiply, numerators, rule
