#!/bin/bash
# Codex fix-loop: repair every item's grounding at the source, regenerate, project, self-check.
cd /private/tmp/claude-501/-Users-tio-Documents-GitHub-hermes/eb956062-1d65-4cdb-91a7-e16c0bd73b2d/scratchpad/t228v2 || exit 1
exec codex exec --json --skip-git-repo-check --sandbox workspace-write --color never \
  -o out/fixloop-final.md \
  "You already ran the 16-item scene task as the comparison arm and wrote twelve grounding-limit notes. The owner has now ruled: fix every item at the root, loop until nothing stinks, and deliver only the freshest best. His words carry the spec: 'Have it loop around to diagnostic mechanics. Just fix what stinks. Markdown files of the articles are gitignored in this repo, so if it needs more context it can just find it. Make it right. Keep notes on how it does that. Some thinness may be that things got unplugged. Others are just slopped together. Ask it to project to new numerals, and then check its own work and diagrams.' THE LOOP, per item (items/items.jsonl, 16 records): (1) DIAGNOSE with your own limit note as the starting point. Classify the stink: description dropped content (the describer transcribed nothing for the base-ten figures N1/N2 — the controller verified transcribed_math is None in their manifest rows); grounding used a registry stand-in instead of the figure's numerals; the trace stops short of the figure's stages; rows missing entirely; or something got unplugged (a capability that exists in Hermes but the item's grounding never called — check via the MCP before declaring absence). (2) FIND THE SOURCE: the research article markdowns are gitignored inside /Users/tio/Documents/GitHub/hermes (read-only for you — search by the item's bibtex_key; the docling harvests under hermes/app/runtime/experiments/ are one home; data/ is another). Read the page around the figure (each item carries page_ref) and recover what the describer dropped: the printed task, all parts of the picture, the student's actual work. (3) FIX: rebuild the item's description and grounding from the source — live hermes MCP calls on the figure's OWN numerals; where Hermes genuinely cannot trace the doing, say so in the notes and ground with what it can do honestly. (4) REGENERATE the scene under the same schema contract; gate and typeset locally (gate_scene.py, typeset.py); revise until it passes and until the scene carries every part of the picture the source shows — the owner noticed the describer covered only part of some images. (5) PROJECT: produce a second scene for the same doing with NEW numerals of your choosing (different from the figure's), same schema. (6) SELF-CHECK both scenes: extract every arithmetic claim and step your diagrams assert and verify them through the hermes MCP (check_math_claim, strategy_trace on the new numerals); a diagram that asserts what the automaton refutes goes back to step 3. Record each check in the notes. DELIVERABLES: out/final/scene/ITEMID.json and out/final/scene/ITEMID_projection.json (final versions only), out/final/svg/ (typeset both), out/final/repair_notes.md (per item: the stink class, the source you read, what you changed, the self-checks with results — thorough notes, the owner will not read raw data dumps but wants the record kept), and out/final/summary.md (one line per item: fixed-from-what, projection verified yes/no). Write nothing outside out/final/. No git operations. End the summary with FIXLOOP_COMPLETE or FIXLOOP_PARTIAL plus counts." \
  > out/fixloop-events.ndjson 2>&1
