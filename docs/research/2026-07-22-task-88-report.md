# Task 88 — formal core follow-ups

## Scope and seam

The frozen change is limited to the difference domain guard, the shared
fraction-component validation seam, and the pass-1 fraction-term builders.

The checker validates invalid fraction components before any registered
fraction clause reaches `ints_to_recs/2`. This shared seam covers all of the
registered fraction claim families and restores `check_math_claim/2`'s
always-succeeds contract without duplicating a guard in each clause. Its
`not_covered` reason states the constraint: the grounded substrate carries
non-negative recollections.

`difference/3` now requires `B > 0, D > 0, Q > 0` before its cross-scaled
calculation. A zero denominator returns `not_covered` with the required
grounded-fraction-domain reason. The sibling audit found `multiplication/3`
already has that guard; `fraction_sum/3` and fraction `comparison/3` already
require non-negative numerators and positive denominators.

The Python `_fraction_term` and `_forced_fraction_term` builders now return
`None` for negative numerator or denominator components. Every fraction-term
call site propagates that `None`, so the pass-1 claim abstains as unchecked
instead of sending a negative fraction term to SWI-Prolog.

## Evidence

The following sandbox-safe probes were run after the edit:

- `difference(3/4, 1/4, 0/0)` returns `not_covered`; the exact rational
  difference is `1/2`, so it no longer vacuously holds.
- A difference sweep over components `1..6`, including exact, simplified,
  and off-by-one claimed results, matched exact-rational truth. Zero-denominator
  mismatches returned `not_covered` rather than a checked verdict.
- Negative numerator and denominator probes for equivalence, n-over-n,
  improper fraction, number-line position, midpoint, multiplication,
  fraction-of-quantity, difference, iterate-to-whole, fraction sum, and
  fraction comparison each returned a `not_covered` dict with the grounded
  substrate constraint.
- The Python builders returned `None` for negative fraction components across
  all eleven fraction-bearing pass-1 shapes (and for a negative forced whole
  number), and `python3 -m py_compile scripts/talkmoves_two_pass.py` passed.

## Files changed

- `hermes/math_claim_checker.pl`
- `scripts/talkmoves_two_pass.py`
- `docs/research/2026-07-22-task-88-report.md`

IMPLEMENTATION_COMPLETE
