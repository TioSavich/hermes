# Task 108 report — institute the MCP

## Delivered

- `hermes/mcp/SETUP.md` gives colleagues the local MCP setup path: checked
  runtime prerequisites, no-pip policy, core-mode registration for Claude Code
  and generic clients, three covered starting calls, optional offline artifacts
  and rebuild commands, and operating limits.
- `python3 -m hermes.mcp.selfcheck` starts one core worker and checks a covered
  monitoring chart, the fraction claim `3/4 = 6/8`, and the contracted
  `count_on_from_larger` strategy trace. It prints `PASS` or `FAIL` for every
  item, supplies a repair step for each failure, and exits nonzero on failure.
- The repository README has one short section that links to the setup guide.

## Validation

- The live environment reported Python 3.12.3 and SWI-Prolog 9.2.9.
- `python3 -m hermes.mcp.selfcheck` passed all three canonical calls.
- A direct standard-input/output JSON-RPC session against
  `python3 hermes/mcp/server.py --mode core` returned successful responses for
  initialization and all three canonical calls.
- `python3 -m py_compile hermes/mcp/selfcheck.py hermes/mcp/server.py` passed.
- The generic `.mcp.json` example parsed with `python3 -m json.tool`, and
  `claude mcp add --help` confirmed the documented stdio registration form.

## Walkthrough

I ran the local runtime checks, the self-check, configuration-shape validation,
and the direct MCP protocol session from this checkout. The initial worker boot
and the three calls took about 29 seconds in this sandbox, so the guide names
the first-call delay plainly.

I did not run `git clone`, because this task prohibits git operations. I did
not run `claude mcp add` itself, because it would change the controller's client
configuration; its installed CLI syntax was verified instead. No repository
history or client configuration was changed.

IMPLEMENTATION_COMPLETE
