# Task 110 report — automata deepening

## Starting state verified in the live checkout

- Contracts: 68 of 172 registry signatures.
- Static transition tables: 171 of 172 signatures.
- Observed-provenance transition tables: none.
- The live strategy_trace input adapter is in hermes/encyclopedia.pl, not hermes_worker.pl. It already decodes fraction_pair objects into fraction_pair(N1, D1, N2, D2) plus unit(whole). The file fence therefore permits no adapter edit: the requested rich input path was already present.
- hermes/mcp/server.py reads knowledge/strategies/automaton_input_contracts.pl when it constructs the strategy_trace schema. No MCP file was changed.

## Completed work

fraction/unit_fraction_partition was admitted with the checked input {"a":1,"b":4}. A worker request returned ok:true, result fraction(1,4), and these ordered step functors:

1. establish_referent_whole
2. partition_whole_into_equal_units
3. select_one_partition_as_unit_fraction
4. preserve_inside_and_iterable_status
5. kernel_trace

They exactly match the existing static path. The contract file now marks that live witness, and the transition-table builder emits the corresponding five provenance(observed(strategy_trace_ok)) facts without replacing the static-source facts.

## Blockers and reconciliation

- fraction/co_denominator_make_base_transfer accepts the already-supported fraction_pair input shape, but its live strategy_trace request did not finish within eight seconds. This is an automaton execution blocker, not an adapter-decoding failure. It remains uncontracted; changing the automaton is outside the file fence.
- fraction/co_denominator_count_on_from_larger has the same registry input shape and remains uncontracted pending a bounded live execution.
- The brief describes three rich, uncontracted fraction signatures including measurement_division. In this checkout, measurement_division was already contracted and its fraction-pair example was present. splitting is not a current registry signature. The live uncontracted fraction rows are unit_fraction_partition and the two co-denominator rows above.
- No pre-existing static-versus-observed disagreement list exists in the checked research report: its three recorded spot checks explicitly report no disagreement. There was consequently no listed case to classify as a code defect, extraction defect, or nondeterminism.

## Verification

- Worker load and live unit_fraction_partition request: passed.
- python3 -m py_compile scripts/research/build_transition_tables.py: passed.
- Builder followed by --check: passed and produced 171 static tables.
- automaton_input_contracts.pl: loaded with 69 contracts and one observed marker.
- Regenerated transition_tables/fraction.pl: loaded in SWI-Prolog.

## Coverage delta

| Surface | Before | After | Delta |
| --- | ---: | ---: | ---: |
| Contracts, fraction | 15 | 16 | +1 |
| Contracts, total | 68 | 69 | +1 |
| Observed table signatures | 0 | 1 | +1 |
| Observed transition facts | 0 | 5 | +5 |
| Static table signatures | 171 | 171 | 0 |

IMPLEMENTATION_PARTIAL
